import React from 'react';
import { View, Text } from 'react-native';

const ProfileMain = () => {
    return (
        <View>
            <Text>프로필 페이지</Text>
        </View>
    );
};

export default ProfileMain;