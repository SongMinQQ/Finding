import React from 'react';
import { View, Text } from 'react-native';

const GladMessage = () => {
    return (
        <View>
            <Text>받은 감사 메세지</Text>
        </View>
    );
};

export default GladMessage;